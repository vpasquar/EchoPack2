{{> global/header}}


<div class="container">
	<div class="row">
		<div class="r-col">
	  	{{!-- {{if login=true}}
	  		{{> global/createforum}}
	  	{{else}}

	  	{{/if}} --}}
	  	{{> global/createform}}
	    {{> global/user}}
	    {{> global/timedate}}
	    {{> global/sentiment}}
	  </div>
	  <div class="l-col">

	  	{{#each box}}
	  	    {{> main/box}}
	  	{{/each}}
	     {{!> main/box}}
	  	 {{!> main/box}}
	  </div>
	  
	</div>  
	
</div>
{{!-- 	{{> global/footer}} --}}